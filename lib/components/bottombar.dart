import 'package:flutter/material.dart';

  BottomNavigationBarItem ourBottomNavigation({ourIcon, ourLabel}) {
    return BottomNavigationBarItem(
          icon: ourIcon,
          label: ourLabel,
          backgroundColor: Colors.white);
  }